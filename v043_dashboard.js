// FACILITY OPS v0.4.3 — OPERATIONS CENTER DASHBOARD
(function(){
  const style=document.createElement('style');
  style.id='facilityOpsDashboard043Style';
  style.textContent=`
  .ops43-grid{display:grid;grid-template-columns:minmax(0,1.65fr) minmax(320px,.78fr);gap:12px;margin-top:12px}.ops43-left,.ops43-right{display:grid;gap:12px;align-content:start}.ops43-charts{display:grid;grid-template-columns:minmax(280px,.8fr) minmax(380px,1.35fr);gap:12px}.ops43-panel{border:1px solid var(--line);background:linear-gradient(145deg,var(--panel),var(--panel-2));border-radius:12px;padding:16px;box-shadow:0 12px 30px rgba(0,0,0,.12)}.ops43-head{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;margin-bottom:14px}.ops43-kicker{font-size:8px;letter-spacing:.17em;font-weight:800;color:var(--muted)}.ops43-head h3{margin:5px 0 0;font-size:14px}.ops43-head small{color:var(--muted);font-size:9px}.ops43-donut-wrap{display:grid;grid-template-columns:130px 1fr;gap:16px;align-items:center}.ops43-donut{width:126px;height:126px;border-radius:50%;display:grid;place-items:center;position:relative;background:conic-gradient(var(--normal) 0 var(--p1),var(--watch) var(--p1) var(--p2),var(--alert) var(--p2) var(--p3),var(--repair) var(--p3) 100%)}.ops43-donut:after{content:'';position:absolute;width:82px;height:82px;background:var(--panel);border-radius:50%;border:1px solid var(--line)}.ops43-donut-center{position:relative;z-index:1;text-align:center}.ops43-donut-center strong{font-size:24px;display:block}.ops43-donut-center span{font-size:8px;color:var(--muted)}.ops43-legend{display:grid;gap:9px}.ops43-legend-row{display:grid;grid-template-columns:9px 1fr auto;gap:8px;align-items:center;font-size:10px}.ops43-dot{width:7px;height:7px;border-radius:50%}.ops43-legend-row span{color:var(--muted)}.ops43-chart{height:180px;position:relative}.ops43-chart svg{width:100%;height:100%;overflow:visible}.ops43-axis{display:flex;justify-content:space-between;margin-top:5px;color:var(--muted);font-size:8px}.ops43-alert-list{display:grid;gap:8px}.ops43-alert{display:grid;grid-template-columns:34px 1fr auto;gap:10px;align-items:center;padding:11px;border:1px solid var(--line);border-radius:10px;background:var(--panel-2)}.ops43-alert-icon{width:32px;height:32px;display:grid;place-items:center;border-radius:8px;font-weight:900}.ops43-alert-icon.high{background:rgba(255,93,98,.13);color:var(--alert)}.ops43-alert-icon.mid{background:rgba(244,189,63,.12);color:var(--watch)}.ops43-alert-icon.info{background:rgba(37,135,248,.12);color:var(--accent)}.ops43-alert strong{font-size:10px;display:block}.ops43-alert small{font-size:8px;color:var(--muted);display:block;margin-top:4px}.ops43-alert time{font-size:8px;color:var(--muted)}.ops43-table-wrap{overflow:auto}.ops43-table{width:100%;border-collapse:collapse;min-width:720px}.ops43-table th{font-size:8px;color:var(--muted);text-align:left;padding:9px;border-bottom:1px solid var(--line);letter-spacing:.08em}.ops43-table td{font-size:9px;color:var(--soft);padding:10px 9px;border-bottom:1px solid var(--line)}.ops43-table tbody tr:hover{background:rgba(37,135,248,.04)}.ops43-type{display:inline-flex;border:1px solid var(--line);background:var(--panel-3);border-radius:999px;padding:4px 7px;font-size:8px;color:var(--soft)}.ops43-title{font-weight:700;color:var(--text);font-size:10px}.ops43-sub{font-size:8px;color:var(--muted);margin-top:3px}.ops43-empty{padding:24px;text-align:center;color:var(--muted);font-size:9px}.ops43-mini{border:1px solid var(--line);background:var(--panel-2);color:var(--soft);height:26px;border-radius:7px;font-size:8px;padding:0 8px}.ops43-mini:hover{border-color:var(--line-strong);color:var(--text)}.ops43-system{display:grid;grid-template-columns:repeat(2,1fr);gap:8px}.ops43-system-card{border:1px solid var(--line);border-radius:9px;padding:11px;background:var(--panel-2)}.ops43-system-card small{font-size:8px;color:var(--muted)}.ops43-system-card strong{display:block;margin-top:6px;font-size:15px}.ops43-system-card em{display:block;margin-top:4px;font-size:8px;color:var(--normal);font-style:normal}
  @media(max-width:1180px){.ops43-grid{grid-template-columns:1fr}.ops43-charts{grid-template-columns:1fr 1fr}}@media(max-width:760px){.ops43-charts{grid-template-columns:1fr}.ops43-donut-wrap{grid-template-columns:1fr}.ops43-donut{margin:auto}}
  `;
  if(!document.getElementById(style.id))document.head.appendChild(style);

  function s(){try{return state||{facilities:[],inspections:[],issues:[],schedules:[]}}catch(_){return{facilities:[],inspections:[],issues:[],schedules:[]}}}
  function e(v){try{return esc(v)}catch(_){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}}
  function fd(v){try{return formatDate(v)}catch(_){return v||'-'}}
  function facilityName(id){const f=s().facilities.find(x=>x.id===id);return f?.name||'-'}
  function nav(view){document.querySelector(`[data-view="${view}"]`)?.click()}
  function ym(date){const d=String(date||'');return /^\d{4}-\d{2}/.test(d)?d.slice(0,7):''}
  function lastMonths(n=6){const now=new Date();const out=[];for(let i=n-1;i>=0;i--){const d=new Date(now.getFullYear(),now.getMonth()-i,1);out.push({key:`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`,label:`${d.getMonth()+1}월`})}return out}
  function lineSvg(values){const w=620,h=170,p=16,max=Math.max(1,...values);const pts=values.map((v,i)=>{const x=p+(w-p*2)*(values.length===1?0:i/(values.length-1));const y=h-p-(h-p*2)*(v/max);return[x,y]});const line=pts.map((p,i)=>`${i?'L':'M'} ${p[0].toFixed(1)} ${p[1].toFixed(1)}`).join(' ');const area=`${line} L ${pts[pts.length-1][0]} ${h-p} L ${pts[0][0]} ${h-p} Z`;return `<svg viewBox="0 0 ${w} ${h}" preserveAspectRatio="none" aria-label="최근 6개월 운영 추이"><defs><linearGradient id="ops43area" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stop-color="#2587f8" stop-opacity=".28"/><stop offset="1" stop-color="#2587f8" stop-opacity="0"/></linearGradient></defs>${[.25,.5,.75,1].map(r=>`<line x1="${p}" x2="${w-p}" y1="${(h-p)-(h-p*2)*r}" y2="${(h-p)-(h-p*2)*r}" stroke="rgba(140,165,190,.12)"/>`).join('')}<path d="${area}" fill="url(#ops43area)"/><path d="${line}" fill="none" stroke="#3693ff" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"/>${pts.map((q,i)=>`<circle cx="${q[0]}" cy="${q[1]}" r="4" fill="#68b6ff"><title>${values[i]}건</title></circle>`).join('')}</svg>`}
  function alertRows(){const st=s();const rows=[];const today=(()=>{try{return todayISO()}catch(_){return new Date().toISOString().slice(0,10)}})();st.issues.filter(x=>x.status!=='done').forEach(x=>rows.push({level:x.severity==='high'?'high':'mid',title:x.title||'고장·민원',sub:`${facilityName(x.facilityId)} · ${x.status==='progress'?'처리중':'미처리'}`,date:x.date||'',view:'issues'}));st.facilities.forEach(f=>{let overdue=false;try{overdue=isOverdue(f)}catch(_){overdue=!!f.nextInspection&&f.nextInspection<today}if(overdue)rows.push({level:'high',title:`점검 기한 초과 · ${f.name}`,sub:`다음 점검 ${fd(f.nextInspection)}`,date:f.nextInspection||'',view:'facilities'});else if(f.status==='alert'||f.status==='watch')rows.push({level:f.status==='alert'?'high':'mid',title:`${f.name} · ${f.status==='alert'?'조치 필요':'주의'}`,sub:f.location||'시설 상태 확인',date:f.nextInspection||'',view:'facilities'})});st.schedules.filter(x=>x.status!=='done'&&x.date===today).forEach(x=>rows.push({level:'info',title:`오늘 일정 · ${x.title}`,sub:`${x.type||'작업'} · ${facilityName(x.facilityId)}`,date:x.date||'',view:'schedule'}));return rows.sort((a,b)=>(a.level==='high'?0:a.level==='mid'?1:2)-(b.level==='high'?0:b.level==='mid'?1:2)||String(a.date).localeCompare(String(b.date))).slice(0,7)}
  function recentRows(){const st=s();return [
    ...st.inspections.map(x=>({type:'점검',date:x.date,title:`${facilityName(x.facilityId)} 점검`,sub:x.note||x.inspector||'점검 기록',status:x.result||'normal',view:'inspections'})),
    ...st.issues.map(x=>({type:'고장·민원',date:x.date,title:x.title||'고장·민원',sub:facilityName(x.facilityId),status:x.status||'open',view:'issues'})),
    ...st.schedules.map(x=>({type:'일정',date:x.date,title:x.title||'작업 일정',sub:`${x.type||'작업'} · ${facilityName(x.facilityId)}`,status:x.status==='done'?'done':'progress',view:'schedule'}))
  ].sort((a,b)=>String(b.date||'').localeCompare(String(a.date||''))).slice(0,9)}
  function statusLabel(v){const m={normal:'정상',watch:'주의',alert:'조치필요',repair:'수리중',open:'미처리',progress:'진행중',done:'완료',planned:'예정'};return m[v]||v||'-'}
  function render(){
    const dash=document.getElementById('view-dashboard');if(!dash)return;
    const st=s();const c={normal:0,watch:0,alert:0,repair:0};st.facilities.forEach(f=>{if(c[f.status]!==undefined)c[f.status]++});const total=st.facilities.length;const issueOpen=st.issues.filter(x=>x.status!=='done').length;const today=(()=>{try{return todayISO()}catch(_){return new Date().toISOString().slice(0,10)}})();const todayTasks=st.schedules.filter(x=>x.date===today&&x.status!=='done').length;let overdue=0;st.facilities.forEach(f=>{try{if(isOverdue(f))overdue++}catch(_){if(f.nextInspection&&f.nextInspection<today)overdue++}});
    const p1=total?c.normal/total*100:0,p2=total?(c.normal+c.watch)/total*100:0,p3=total?(c.normal+c.watch+c.alert)/total*100:0;
    const months=lastMonths();const trend=months.map(m=>st.inspections.filter(x=>ym(x.date)===m.key).length+st.issues.filter(x=>ym(x.date)===m.key).length+st.schedules.filter(x=>ym(x.date)===m.key).length);
    const alerts=alertRows(),recent=recentRows();
    dash.innerHTML=`
      <div class="status-grid">
        ${typeof statusCard==='function'?statusCard('정상 시설',c.normal,'정상 운영 중','normal','normal'):`<div class="status-card normal"><div class="label">정상 시설</div><div class="value">${c.normal}</div></div>`}
        ${typeof statusCard==='function'?statusCard('주의·조치',c.watch+c.alert,'관찰 및 조치 대상','watch','watch'):`<div class="status-card watch"><div class="label">주의·조치</div><div class="value">${c.watch+c.alert}</div></div>`}
        ${typeof statusCard==='function'?statusCard('미처리 이슈',issueOpen,'고장·민원 미완료','alert','alert'):`<div class="status-card alert"><div class="label">미처리 이슈</div><div class="value">${issueOpen}</div></div>`}
        ${typeof statusCard==='function'?statusCard('오늘 작업',todayTasks,'오늘 예정된 작업','repair','repair'):`<div class="status-card repair"><div class="label">오늘 작업</div><div class="value">${todayTasks}</div></div>`}
      </div>
      <div class="ops43-grid">
        <div class="ops43-left">
          <div class="ops43-charts">
            <section class="ops43-panel"><div class="ops43-head"><div><div class="ops43-kicker">FACILITY STATUS</div><h3>시설 상태 분포</h3></div><small>${total}개 시설</small></div><div class="ops43-donut-wrap"><div class="ops43-donut" style="--p1:${p1}%;--p2:${p2}%;--p3:${p3}%"><div class="ops43-donut-center"><strong>${total}</strong><span>TOTAL</span></div></div><div class="ops43-legend">${[['normal','정상',c.normal,'var(--normal)'],['watch','주의',c.watch,'var(--watch)'],['alert','조치필요',c.alert,'var(--alert)'],['repair','수리중',c.repair,'var(--repair)']].map(r=>`<div class="ops43-legend-row"><i class="ops43-dot" style="background:${r[3]}"></i><span>${r[1]}</span><b>${r[2]}</b></div>`).join('')}</div></div></section>
            <section class="ops43-panel"><div class="ops43-head"><div><div class="ops43-kicker">OPERATIONS TREND</div><h3>최근 6개월 운영 기록 추이</h3></div><small>점검·민원·일정 통합</small></div><div class="ops43-chart">${lineSvg(trend)}</div><div class="ops43-axis">${months.map(m=>`<span>${m.label}</span>`).join('')}</div></section>
          </div>
          <section class="ops43-panel"><div class="ops43-head"><div><div class="ops43-kicker">RECENT OPERATIONS</div><h3>최근 작업 테이블</h3></div><small>최신 ${recent.length}건</small></div><div class="ops43-table-wrap">${recent.length?`<table class="ops43-table"><thead><tr><th>유형</th><th>작업/기록</th><th>상태</th><th>일자</th><th></th></tr></thead><tbody>${recent.map(r=>`<tr><td><span class="ops43-type">${e(r.type)}</span></td><td><div class="ops43-title">${e(r.title)}</div><div class="ops43-sub">${e(r.sub)}</div></td><td>${typeof badge==='function'?badge(r.status):e(statusLabel(r.status))}</td><td>${fd(r.date)}</td><td><button class="ops43-mini" data-ops-nav="${r.view}">열기</button></td></tr>`).join('')}</tbody></table>`:'<div class="ops43-empty">최근 작업 기록이 없습니다.</div>'}</div></section>
        </div>
        <aside class="ops43-right">
          <section class="ops43-panel"><div class="ops43-head"><div><div class="ops43-kicker">ALERT CENTER</div><h3>실시간 경고센터</h3></div><small>${alerts.length}건 표시</small></div><div class="ops43-alert-list">${alerts.length?alerts.map(a=>`<div class="ops43-alert"><div class="ops43-alert-icon ${a.level}">${a.level==='high'?'!':a.level==='mid'?'△':'•'}</div><div><strong>${e(a.title)}</strong><small>${e(a.sub)}</small></div><button class="ops43-mini" data-ops-nav="${a.view}">확인</button></div>`).join(''):'<div class="ops43-empty">현재 우선 경고가 없습니다.</div>'}</div></section>
          <section class="ops43-panel"><div class="ops43-head"><div><div class="ops43-kicker">SYSTEM SNAPSHOT</div><h3>운영 스냅샷</h3></div><small>현재 기준</small></div><div class="ops43-system"><div class="ops43-system-card"><small>점검 기한 초과</small><strong>${overdue}</strong><em>${overdue?'확인 필요':'정상'}</em></div><div class="ops43-system-card"><small>수리 진행 시설</small><strong>${c.repair}</strong><em>${c.repair?'작업 진행중':'없음'}</em></div><div class="ops43-system-card"><small>미처리 민원</small><strong>${issueOpen}</strong><em>${issueOpen?'처리 대기':'정상'}</em></div><div class="ops43-system-card"><small>오늘 작업</small><strong>${todayTasks}</strong><em>${todayTasks?'일정 확인':'없음'}</em></div></div></section>
        </aside>
      </div>`;
    dash.querySelectorAll('[data-ops-nav]').forEach(b=>b.onclick=()=>nav(b.dataset.opsNav));
    dash.querySelectorAll('[data-dashboard-filter]').forEach(b=>b.onclick=()=>{try{currentFilter=b.dataset.dashboardFilter;nav('facilities')}catch(_){nav('facilities')}});
  }
  try{renderDashboard=render;window.renderDashboard=render}catch(_){window.renderDashboard=render}
  const oldRenderView=typeof renderView==='function'?renderView:null;if(oldRenderView&&!oldRenderView.__ops43){const wrapped=function(v){if(v==='dashboard')return render();return oldRenderView(v)};wrapped.__ops43=true;try{renderView=wrapped;window.renderView=wrapped}catch(_){}}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>{if(document.getElementById('view-dashboard')?.classList.contains('active'))render()},{once:true});else if(document.getElementById('view-dashboard')?.classList.contains('active'))render();
})();
